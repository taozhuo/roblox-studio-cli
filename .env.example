# Bakable Daemon Configuration

# Port for HTTP/WebSocket server (default: 4849)
BAKABLE_PORT=4849

# Path to sync repo (default: ./bakable-repo)
BAKABLE_REPO=./bakable-repo

# Auth token (auto-generated if not set)
# BAKABLE_TOKEN=your-custom-token

# Roblox Open Cloud API Key (required for cloud tools)
# Get from: https://create.roblox.com/dashboard/credentials
# Requires DataStore + Place permissions
ROBLOX_OPEN_CLOUD_KEY=your-open-cloud-api-key

# For MCP stdio mode - URL of running daemon
BAKABLE_DAEMON_URL=http://127.0.0.1:4849
